export const makeExtract = (text, max = 75) => {
  // if (text.length > max) {
  //   // XXX needs more thought since this might chop off markdown
  //   return text.substring(0, max - 1) + '\u2026'
  // }
  return text
}
